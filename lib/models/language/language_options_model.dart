import 'package:flutter/material.dart';

// Model class to hold menu option data (language and icon)
class LanguageOptionsModel {
  String key;
  String value;
  IconData icon;

  LanguageOptionsModel({this.key, this.value, this.icon});
}